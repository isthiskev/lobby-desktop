{
  "identifier": "default",
  "description": "Main window — the Lobby web app renders custom window chrome (frameless shell), so it may minimize/close/drag the window. No other IPC.",
  "windows": ["main"],
  "remote": {
    "urls": ["https://lobby-web.vercel.app"]
  },
  "permissions": [
    "core:window:allow-minimize",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
